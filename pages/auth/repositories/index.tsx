import {
    ApolloClient,
    InMemoryCache,
    gql,
} from '@apollo/client'

const API_URL: string = 'https://api.github.com/graphql'

export default function Repositories() {

    return (
        <div>

        </div>
    )
}
